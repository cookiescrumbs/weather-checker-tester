{
  "name": "weather-checker-tester",
  "version": "0.0.1",
  "description": "A test framework that tests the weather checker app",
  "scripts": {
    "cypress:open": "cypress open",
    "cypress:run": "cypress run",
    "build:build-env": "docker-compose build",
    "run:build-env": "docker-compose run build-env"
  },
  "author": "Steven Cook",
  "license": "ISC",
  "dependencies": {
    "cypress": "^4.12.1",
    "moment": "^2.27.0",
    "typescript": "^3.9.7"
  }
}
